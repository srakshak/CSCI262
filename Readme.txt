Following command can be used to compile the program.

CC main.cpp ActivityEngine.cpp AnalysisEngine.cpp AlertEngine.cpp -std=c++11 -o Traffic

use the following command to run the program

./Traffic

input filenames will be prompted one by one.
